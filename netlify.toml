[build]
  publish = "dist/spa"
  command = "npm run build:client"

# Handle Single Page Application (SPA) routing so direct links work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Example API Proxy (Uncomment and adjust if you have a deployed backend)
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-backend-api.com/api/:splat"
#   status = 200
#   force = true